name: Update File References
on: workflow_dispatch
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PERSONAL_TOKEN }}
          fetch-depth: 0

      - name: Update YAML files
        run: |
          for i in {1..9}; do
            if [ -f "C$i.yaml" ]; then
              sed -i 's/Camoufox\.zip/C
